# EC2 Setup (manual / bootstrap steps)

1. Launch an Ubuntu 20.04/22.04 EC2 instance in the public subnet.
2. Attach an IAM role if using S3 to store model artifacts or Docker registry access.
3. SSH into the instance:
